---
allowed-tools: Bash(pnpm:*), Bash(node:*)
description: Generate HTML docs from markdown into reports/
---

Run the docs generation script. It generates all HTML reports, builds the index hub, and auto-opens it in the browser.

```bash
pnpm run docs
```
