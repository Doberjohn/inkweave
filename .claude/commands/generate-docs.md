---
allowed-tools: Bash(pnpm:*), Bash(node:*)
description: Generate HTML docs from markdown into reports/
---

Run the docs generation script and confirm output:

```bash
pnpm run docs
```

After generation, remind the user to open `reports/index.html` in their browser.
